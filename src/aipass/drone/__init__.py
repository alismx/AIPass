"""aipass.drone — Command routing, @resolution, module discovery.

The backbone of AIPass. Routes `drone @module command` to the correct
module, resolves @ targets to paths, discovers installed modules.
"""
