# Templates

Branch-specific templates for `SPAWN`.

Any templates this branch provides to the system or uses internally. Examples: plan templates (flow), trinity templates (memory), test templates (seedgo).
