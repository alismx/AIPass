"""Agent creation, registry, and scaffolding.

Powers `aipass init` (builds all citizens on first run) and
`drone @memory create agent "X"` (adds new branches later).

Creates: passport.json, flow.local.md, birth certificate, registry entry.
"""
